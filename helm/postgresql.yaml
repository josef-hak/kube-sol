postgresql:
  global:
    security:
      allowInsecureImages: true
  image:
    repository: bitnamilegacy/postgresql
  auth:
    postgresPassword: pgpasswd
    username: user
    password: passwd
    database: pgdb
  readReplicas:
    replicaCount: 2